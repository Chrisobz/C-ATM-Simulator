#include <iostream>
using namespace std;

int main() {

    int x, y;
    int sum;
    cout<<"choose number: ";
    cin >> x;
    cout<<"second number: ";
    cin >> y;
    sum = x + y;
    cout << "sum is:";
    cout <<  sum ;

    return 0;
}